Placeholder. Other vehicles: detect them, predict where they go, plan around them.

Start from camera/lidar tracks, not from `world.get_actors()` as a driving input. Simulator actors are fine as labels.
